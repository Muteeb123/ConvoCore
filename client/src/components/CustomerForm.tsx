// CustomerForm component placeholder
// - Purpose: UI form for adding a new client (customer)
// - Fields to include (min): companyName, contactName, contactEmail, phone, website, city, country, industry, notes
// - Validation: required fields (companyName), email format for contactEmail, optional phone format validation
// - Behavior: on submit, POST validated payload to `/api/customers` and handle errors
// - UX: show inline validation errors, a loading state, and success toast/modal on create
// - After successful creation: navigate back to customers list or refresh customers query
// - Consider file uploads: if adding `customerFiles`, integrate file input and multipart/form-data

export default function CustomerFormPlaceholder() {
  // This file intentionally contains no implementation code — only guidance comments.
  return null as any;
}
